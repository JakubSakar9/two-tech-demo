- Compute normal and powdered snow height using precipitation calculations
- Compute temperature texture based on altitude and direct sun exposure
- Compute snow melting
- Snow diffusion
- Wind snow transfer
- Use different step sounds when walking on grass
- Create a data structure that stores past footprints
- When encoutering a past chunk reconstruct footprints using a compute shader (naive a solution for now)
- Decrease footprint depth when changing a chunk if it is snowing
- Optimize footprints render distance by only using textures for the chunks where footprints are rendered
- Configurable precipitation intensity
- Sample only a 2D texture to get wind velocity on CPU
- Limit a number of chunks with footprints and discard the latest ones (speedup)
- Update the discard policy to not discard textures which are nearby
- [Visual Improvements]
- [Test with users]
- [Make everything giant (terrain LODs)]
- [Vegetation/obstacles]
- [Experiment with hardware tesselation to improve performance]
- [UI/Gamey stuff]
- [Add procedural animation]

Questions:
- To what extent should "snow transfer" be simulated? Should it happen in real-time?
- The wind vector field has very low precision, would it make sense to make base wind velocity constant in the vector field regardless of altitude and only change the vector field strength on the CPU side?
- If snow transfer should happen in Realtime, it would no longer be possible to sample the heightmap on the CPU. How should I then handle/compute displacement textures?
- In case of offline snow transfer computation, would it be instead possible to simulate long-term wind transfer in the precipitation stage?
